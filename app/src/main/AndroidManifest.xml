<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="my.iooogik.Book">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <receiver android:name="my.iooogik.Book.NotificationReceiver" />

        <activity android:name="my.iooogik.Book.StandartNote" />
        <activity android:name="my.iooogik.Book.QR_Demo" />
        <activity android:name="my.iooogik.Book.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="my.iooogik.Book.registration.Registration" />
        <activity android:name="my.iooogik.Book.registration.SignIn" />
        <activity
            android:name="my.iooogik.Book.BarcodeCaptureActivity"
            android:label="Read Barcode" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>